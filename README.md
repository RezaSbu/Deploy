# پروژه فرم ثبت نام کاربران

یک پروژه ساده و زیبا برای ثبت نام کاربران با استفاده از **React** برای فرانت‌اند و **Node.js** برای بک‌اند، که به **Supabase** متصل می‌شود. این پروژه کاملاً داکرایز شده و آماده اجرا روی Docker Desktop است.

## 📁 ساختار پروژه

```
user-form-project/
├── Backend/              # بک‌اند با Node.js
│   ├── server.js         # فایل اصلی سرور
│   ├── package.json      # وابستگی‌های Node.js
│   ├── .env              # تنظیمات Supabase
│   ├── .env.example      # نمونه فایل تنظیمات
│   └── Dockerfile        # فایل داکر بک‌اند
├── FrontEnd/             # فرانت‌اند با React
│   ├── public/
│   │   └── index.html
│   ├── src/
│   │   ├── App.js        # کامپوننت اصلی
│   │   ├── App.css       # استایل‌ها
│   │   └── index.js      # نقطه ورود React
│   ├── package.json
│   ├── nginx.conf        # تنظیمات Nginx
│   └── Dockerfile        # فایل داکر فرانت‌اند
├── Database/
│   └── Database.txt      # کد SQL برای Supabase
├── docker-compose.yml    # فایل تنظیمات Docker Compose
└── README.md             # این فایل
```

## 🐳 راهنمای کامل اجرا با Docker

این بخش شامل راهنمای جامع برای اجرا، مدیریت و استقرار پروژه با استفاده از Docker است. با دنبال کردن این دستورالعمل‌ها، می‌توانید پروژه را به راحتی روی سیستم خودتان اجرا کنید یا آن را روی یک سرور واقعی مستقر (Deploy) کنید.

### ۱. پیش‌نیازهای سیستم

قبل از شروع، باید نرم‌افزارهای زیر روی سیستم شما نصب باشد. این نرم‌افزارها برای اجرای پروژه در محیط Docker ضروری هستند و بدون آن‌ها امکان اجرای پروژه وجود ندارد.

#### ۱.۱ Docker Desktop

Docker Desktop یک نرم‌افزار رابط گرافیکی برای مدیریت کانتینرهای Docker است. برای دانلود و نصب آن، مراحل زیر را دنبال کنید:

ابتدا به وب‌سایت رسمی Docker به آدرس [docker.com](https://www.docker.com/products/docker-desktop) بروید. سپس روی دکمه Download for Windows کلیک کنید و فایل نصب را دانلود کنید. پس از دانلود، فایل نصب را اجرا کنید و طبق مراحل نصب پیش بروید. در طول نصب، گزینه‌های پیش‌فرض را قبول کنید. پس از اتمام نصب، از شما خواسته می‌شود که سیستم را ری‌استارت کنید. سیستم را ری‌استارت کنید و پس از بالا آمدن، Docker Desktop را اجرا کنید.

#### ۱.۲ بررسی نصب بودن Docker

پس از نصب، برای اطمینان از نصب صحیح، ترمینال (Command Prompt یا PowerShell) را باز کنید و دستورات زیر را اجرا کنید:

```bash
docker --version
docker-compose --version
```

اگر نصب موفقیت‌آمیز باشد، خروجی مشابه زیر خواهید دید:

```
Docker version 24.0.7
Docker Compose version v2.21.0
```

اعداد نسخه ممکن است متفاوت باشند، اما مهم این است که دستور بدون خطا اجرا شود و شماره نسخه نمایش داده شود.

### ۲. آماده‌سازی پروژه برای اجرا

پس از اطمینان از نصب بودن Docker، باید پروژه را آماده کنید. این مرحله شامل کپی پروژه و تنظیم متغیرهای محیطی است.

#### ۲.۱ کپی پروژه روی سیستم

پوشه پروژه را از هر کجا که هست به یک مسیر مناسب روی سیستم خود کپی کنید. به عنوان مثال، می‌توانید پوشه را روی دسکتاپ خود قرار دهید:

```
C:\Users\DELL\Desktop\user-form-project
```

دقت کنید که تمام فایل‌ها و پوشه‌های پروژه باید در این مسیر وجود داشته باشند. ساختار پوشه‌ها نباید تغییر کند.

#### ۲.۲ تنظیم فایل متغیرهای محیطی (.env)

فایل `.env` حاوی اطلاعات حساس مربوط به اتصال به Supabase است. این فایل باید در پوشه `Backend` وجود داشته باشد. اگر این فایل وجود ندارد، یک فایل متنی جدید با نام `.env` در پوشه `Backend` بسازید و محتوای زیر را در آن قرار دهید:

```env
SUPABASE_URL=https://tomjlybknzajmokcoybc.supabase.co
SUPABASE_KEY=کلید_عمومی_Supabase_خود_را_اینجا_قرار_دهید
```

برای دریافت کلید عمومی Supabase، به پنل Supabase بروید و از مسیر Settings > API کلید anon را کپی کنید. دقت کنید که کلید را به صورت کامل و بدون فاصله اضافی وارد کنید.

#### ۲.۳ بررسی فایل docker-compose.yml

فایل `docker-compose.yml` در پوشه اصلی پروژه قرار دارد و تنظیمات مربوط به اجرای کانتینرها را در خود دارد. این فایل از قبل تنظیم شده است و نیازی به تغییر ندارد، مگر اینکه بخواهید پورت‌ها را تغییر دهید.

### ۳. اجرای پروژه با Docker Compose

پس از آماده‌سازی، می‌توانید پروژه را اجرا کنید. Docker Compose تمام کانتینرهای مورد نیاز را می‌سازد و اجرا می‌کند.

#### ۳.۱ باز کردن ترمینال

ترمینال (Git Bash، Command Prompt یا PowerShell) را باز کنید و به پوشه پروژه بروید:

```bash
cd C:\Users\DELL\Desktop\user-form-project
```

اگر از Git Bash استفاده می‌کنید، از مسیر لینوکسی استفاده کنید:

```bash
cd /c/Users/DELL/Desktop/user-form-project
```

#### ۳.۲ اجرای دستور ساخت و اجرا

برای ساخت و اجرای پروژه، دستور زیر را اجرا کنید:

```bash
docker-compose up --build
```

این دستور برای بار اول زمان بیشتری طول می‌کشد، چون باید Image‌های پایه (Node.js، Nginx) را دانلود کند و پروژه را بسازد. بسته به سرعت اینترنت، این فرآیند ممکن است ۵ تا ۱۵ دقیقه طول بکشد.

#### ۳.۳ مشاهده پیشرفت ساخت

پس از اجرای دستور، ترمینال لاگ‌های ساخت را نمایش می‌دهد. اگر همه چیز درست پیش برود، در نهایت پیام‌های زیر را خواهید دید:

```
[+] Running 2/2
 ✔ Container user-form-backend   Started
 ✔ Container user-form-frontend  Started
```

این پیام یعنی هر دو کانتینر (بک‌اند و فرانت‌اند) با موفقیت اجرا شدند.

### ۴. دسترسی به برنامه

پس از اجرای موفقیت‌آمیز، می‌توانید از طریق مرورگر به برنامه دسترسی داشته باشید.

#### ۴.۱ فرانت‌اند (React)

برای مشاهده رابط کاربری فرم ثبت‌نام، مرورگر را باز کنید و به آدرس زیر بروید:

```
http://localhost:3000
```

این آدرس صفحه اصلی برنامه را نمایش می‌دهد که شامل فرم ثبت‌نام با فیلدهای نام و نام خانوادگی، ایمیل و سن است.

#### ۴.۲ بک‌اند (API)

برای دسترسی مستقیم به API، می‌توانید از آدرس زیر استفاده کنید:

```
http://localhost:8000
```

این آدرس یک پیام خوش‌آمدگویی JSON نمایش می‌دهد که لیست تمام endpoint‌های موجود را نشان می‌دهد.

#### ۴.۳ مستندات API

برای مشاهده لیست کامل endpoint‌ها و نحوه استفاده از آن‌ها، به آدرس زیر بروید:

```
http://localhost:8000
```

پاسخ این آدرس شامل تمام مسیرهای موجود در API است.

### ۵. مدیریت کانتینرها

پس از اجرای پروژه، ممکن است نیاز به مدیریت کانتینرها داشته باشید. این بخش شامل دستورات مفید برای مدیریت روزانه است.

#### ۵.۱ مشاهده وضعیت کانتینرها

برای دیدن اینکه کدام کانتینرها در حال اجرا هستند و وضعیت آن‌ها چیست، از دستور زیر استفاده کنید:

```bash
docker-compose ps
```

خروجی این دستور مشابه زیر خواهد بود:

```
NAME                 STATUS    PORTS
user-form-backend    Up        0.0.0.0:8000->8000/tcp
user-form-frontend   Up        0.0.0.0:3000->80/tcp
```

#### ۵.۲ توقف پروژه

برای توقف تمام کانتینرها بدون حذف آن‌ها، از دستور زیر استفاده کنید:

```bash
docker-compose down
```

این دستور کانتینرها را متوقف می‌کند، اما Image‌ها و Network را حفظ می‌کند.下次 اجرا سریع‌تر خواهد بود.

#### ۵.۳ توقف و حذف کامل

برای توقف و حذف تمام کانتینرها، Image‌ها و حجم‌ها، از دستور زیر استفاده کنید:

```bash
docker-compose down -v
```

از این دستور زمانی استفاده کنید که می‌خواهید همه چیز را پاک کنید و از اول بسازید.

#### ۵.۴ اجرا در پس‌زمینه

اگر می‌خواهید پروژه در پس‌زمینه اجرا شود و ترمینال آزاد باشد، از دستور زیر استفاده کنید:

```bash
docker-compose up -d --build
```

پس از اجرا، می‌توانید ترمینال را ببندید و برنامه همچنان اجرا می‌شود.

#### ۵.۵ مشاهده لاگ‌ها

برای دیدن لاگ‌های برنامه و عیب‌یابی، از دستورات زیر استفاده کنید:

مشاهده تمام لاگ‌ها:

```bash
docker-compose logs -f
```

مشاهده لاگ‌های فقط یک سرویس:

```bash
docker-compose logs -f backend
docker-compose logs -f frontend
```

برای خروج از حالت مشاهده لاگ‌ها، کلیدهای Ctrl+C را فشار دهید.

### ۶. استقرار (Deployment) روی سرور واقعی

برای استقرار پروژه روی یک سرور واقعی (مثلاً سرور ابری، VPS یا سرور اختصاصی)، مراحل زیر را دنبال کنید.

#### ۶.۱ آماده‌سازی سرور

سرور باید دارای سیستم‌عامل لینوکس (ترجیحاً Ubuntu 20.04 یا 22.04) باشد و Docker روی آن نصب باشد. اگر Docker روی سرور نصب نیست، مراحل زیر را دنبال کنید:

به سرور وصل شوید و دستورات زیر را اجرا کنید:

```bash
sudo apt update
sudo apt install apt-transport-https ca-certificates curl software-properties-common
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
sudo apt update
sudo apt install docker-ce docker-ce-cli containerd.io docker-compose-plugin
```

پس از نصب، مطمئن شوید که Docker در حال اجرا است:

```bash
sudo systemctl status docker
```

#### ۶.۲ انتقال فایل‌ها به سرور

فایل‌های پروژه را به سرور منتقل کنید. می‌توانید از SCP یا SFTP استفاده کنید:

```bash
scp -r user-form-project user@your-server-ip:/home/user/
```

یا اگر از Git استفاده می‌کنید، پروژه را روی سرور کلون کنید:

```bash
git clone https://github.com/your-username/user-form-project.git
cd user-form-project
```

#### ۶.۳ تنظیم متغیرهای محیطی روی سرور

فایل `.env` را در پوشه `Backend` بسازید و کلیدهای Supabase را وارد کنید. دقت کنید که در محیط production، بهتر است از کلید service_role به جای کلید anon استفاده کنید:

```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=service_role_key_از_Supabase
```

#### ۶.۴ اجرای پروژه روی سرور

پس از انتقال فایل‌ها، پروژه را بسازید و اجرا کنید:

```bash
docker-compose up -d --build
```

پس از اتمام ساخت، برنامه روی پورت‌های زیر در دسترس خواهد بود:

- فرانت‌اند: http://your-server-ip:3000
- بک‌اند: http://your-server-ip:8000

#### ۶.۵ تنظیم Nginx به عنوان Reverse Proxy (اختیاری)

برای استفاده از دامنه اختصاصی و پورت ۸۰ (HTTP) یا ۴۴۳ (HTTPS)، می‌توانید از Nginx استفاده کنید. Nginx را نصب کنید:

```bash
sudo apt install nginx
```

سپس یک فایل پیکربندی برای دامنه خود بسازید:

```bash
sudo nano /etc/nginx/sites-available/user-form-project
```

محتوای زیر را در فایل قرار دهید:

```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }

    location /api {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

فایل را ذخیره کنید و لینک آن را به sites-enabled بسازید:

```bash
sudo ln -s /etc/nginx/sites-available/user-form-project /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

#### ۶.۶ تنظیم HTTPS با Certbot (اختیاری)

برای امن کردن ارتباط با گواهی SSL، می‌توانید از Certbot استفاده کنید:

```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com -d www.your-domain.com
```

پس از تکمیل، سایت شما با HTTPS در دسترس خواهد بود.

#### ۶.۷ تنظیم اجرای خودکار پس از ری‌استارت

برای اینکه پروژه پس از ری‌استارت سرور به صورت خودکار اجرا شود، سرویس Docker را فعال کنید:

```bash
sudo systemctl enable docker
sudo systemctl enable docker-compose
```

اگر از docker-compose استفاده می‌کنید، می‌توانید یک systemd service بسازید:

```bash
sudo nano /etc/systemd/system/user-form-project.service
```

محتوای زیر را قرار دهید:

```ini
[Unit]
Description=User Form Project
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
WorkingDirectory=/path/to/user-form-project
ExecStart=/usr/bin/docker-compose up -d
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
```

سپس سرویس را فعال کنید:

```bash
sudo systemctl daemon-reload
sudo systemctl enable user-form-project
sudo systemctl start user-form-project
```

### ۷. عیب‌یابی مشکلات رایج

این بخش شامل راه‌حل‌های مشکلات رایجی است که ممکن است در حین اجرا با آن‌ها مواجه شوید.

#### ۷.۱ خطای پورت اشغال

اگر پیام خطایی مبنی بر اشغال بودن پورت دریافت کردید، یعنی یک برنامه دیگر از همان پورت استفاده می‌کند. برای رفع این مشکل، فایل `docker-compose.yml` را ویرایش کنید و پورت‌ها را تغییر دهید:

```yaml
services:
  backend:
    ports:
      - "8001:8000"  # پورت جدید به جای 8000

  frontend:
    ports:
      - "3001:80"    # پورت جدید به جای 3000
```

پس از تغییر، پروژه را دوباره بسازید:

```bash
docker-compose down
docker-compose up --build
```

#### ۷.۲ خطای اتصال به Supabase

اگر در لاگ‌ها خطای مربوط به Supabase دیدید، موارد زیر را بررسی کنید:

ابتدا مطمئن شوید که فایل `.env` در پوشه `Backend` وجود دارد و کلیدها صحیح هستند. سپس بررسی کنید که کلید Supabase منقضی نشده باشد. در نهایت، مطمئن شوید که جدول users در Supabase با کد SQL ساخته شده است.

#### ۷.۳ کانتینر اجرا نمی‌شود

اگر کانتینر اجرا نمی‌شود، لاگ‌ها را بررسی کنید:

```bash
docker-compose logs
docker-compose logs backend
docker-compose logs frontend
```

اگر خطایی در لاگ‌ها وجود دارد، آن را بررسی کنید. خطاها معمولاً شامل اطلاعات مفیدی برای رفع مشکل هستند.

#### ۷.۴ خطای Memory در Docker Desktop

اگر Docker Desktop با خطای حافظه مواجه شد، تنظیمات Docker Desktop را باز کنید و میزان حافظه اختصاص داده شده را افزایش دهید. به Settings > Resources > Memory بروید و مقدار را به حداقل ۴ گیگابایت افزایش دهید.

#### ۷.۵ خطای TLS handshake timeout

این خطا معمولاً به دلیل مشکل در اتصال به Docker Registry رخ می‌دهد. برای رفع، VPN را روشن کنید و دوباره امتحان کنید. اگر باز هم مشکل بود، DNS سرور را تغییر دهید:

```
8.8.8.8
8.8.4.4
```

### ۸. به‌روزرسانی پروژه

برای به‌روزرسانی پروژه به نسخه جدید، مراحل زیر را دنبال کنید:

ابتدا از فایل‌های مهم نسخه پشتیبان بگیرید. سپس نسخه جدید پروژه را دانلود کنید و فایل `.env` را به پوشه جدید کپی کنید. پس از آن، کانتینرهای قدیمی را متوقف و حذف کنید و نسخه جدید را بسازید:

```bash
docker-compose down
docker-compose up --build -d
```

### ۹. پشتیبان‌گیری

برای پشتیبان‌گیری از داده‌ها، به پنل Supabase بروید و از بخش Backups نسخه پشتیبان بسازید. همچنین می‌توانید از جدول users خروجی SQL بگیرید:

```bash
pg_dump "postgresql://user:password@host:5432/db" > backup.sql
```

### ۱۰. خلاصه دستورات Docker Compose

در جدول زیر، خلاصه‌ای از دستورات پرکاربرد Docker Compose آورده شده است:

| دستور | توضیح |
|-------|--------|
| `docker-compose up --build` | ساخت و اجرا |
| `docker-compose up -d` | اجرا در پس‌زمینه |
| `docker-compose down` | توقف |
| `docker-compose down -v` | توقف با حذف حجم‌ها |
| `docker-compose ps` | مشاهده وضعیت |
| `docker-compose logs` | مشاهده لاگ‌ها |
| `docker-compose logs -f` | مشاهده لاگ‌ها به صورت زنده |
| `docker-compose restart` | ری‌استارت کانتینرها |
| `docker-compose exec backend ls` | اجرای دستور در کانتینر |

با دنبال کردن این راهنما، می‌توانید پروژه را به راحتی اجرا، مدیریت و مستقر کنید. اگر سوالی داشتید یا با مشکلی مواجه شدید، با من تماس بگیرید.

## 🚀 اجرای توسعه (بدون Docker)

### راه‌اندازی بک‌اند

```bash
cd Backend
npm install
npm start
```

سرور روی آدرس `http://localhost:8000` اجرا می‌شود.

### راه‌اندازی فرانت‌اند

```bash
cd FrontEnd
npm install
npm start
```

فرانت‌اند روی آدرس `http://localhost:3000` اجرا می‌شود.

## 📡 API Endpoints

| متد | مسیر | توضیحات |
|-----|------|---------|
| GET | `/` | ریشه API |
| POST | `/users` | ثبت کاربر جدید |
| GET | `/users` | دریافت تمام کاربران |
| GET | `/users/:id` | دریافت یک کاربر |
| DELETE | `/users/:id` | حذف یک کاربر |
| GET | `/stats` | آمار کلی |

## 🎨 ویژگی‌ها

- طراحی زیبا و مدرن با گرادینت بنفش و آبی
- کاملاً ریسپانسیو برای موبایل و دسکتاپ
- پشتیبانی کامل از زبان فارسی (RTL) با فونت وزیرمتن
- اعتبارسنجی فرم سمت کلاینت و سرور
- نمایش پیام‌های موفقیت و خطا با طراحی جذاب
- لیست کاربران ثبت شده با قابلیت به‌روزرسانی زنده
- انیمیشن‌های نرم و روان در تمام بخش‌ها
- کاملاً داکرایز شده برای استقرار آسان

## 🛠️ تکنولوژی‌ها

**فرانت‌اند:**
- React 18
- Axios
- CSS3 (طراحی سفارشی)
- Nginx (برای production)

**بک‌اند:**
- Node.js 18
- Express
- Supabase Client

**دیتابیس:**
- Supabase (PostgreSQL)

**داکر:**
- Docker Desktop
- Docker Compose
- Node.js Alpine
- Nginx Alpine

## 📝 نکات مهم

۱. حتماً اول کد SQL را در Supabase اجرا کنید تا جدول users ساخته شود
۲. فایل `.env` را با کلیدهای صحیح Supabase پر کنید
۳. در Docker Desktop، مطمئن شوید که Docker در حال اجرا است
۴. اگر پورت‌های ۳۰۰۰ یا ۸۰۰۰ در سیستم شما اشغال هستند، در `docker-compose.yml` تغییر دهید
۵. برای محیط production، کلید service_role Supabase را به جای anon key استفاده کنید

## ❓ عیب‌یابی

### خطای پورت اشغال

اگر پیام خطای پورت اشغال دریافت کردید، پورت را در `docker-compose.yml` تغییر دهید:

```yaml
ports:
  - "3001:80"  # به جای 3000
```

### خطای اتصال به Supabase

مطمئن شوید که فایل `.env` در پوشه `Backend` وجود دارد و کلیدها صحیح هستند.

### کانتینر اجرا نمی‌شود

لاگ‌ها را بررسی کنید:

```bash
docker-compose logs
```

---

ساخته شده با ❤️























































ترمینال را باز کنید و دستورات زیر را اجرا کنید:

bash
cd C:\Users\DELL\Desktop\user-form-project
docker-compose up --build
مرحله ۳: دسترسی
پس از اتمام build:

فرانت‌اند: http://localhost:3000
بک‌اند: http://localhost:8000
📋 دستورات مفید
دستور	توضیح
docker-compose up --build	ساخت و اجرا
docker-compose down	توقف
docker-compose up -d	اجرا در پس‌زمینه
docker-compose logs -f	مشاهده لاگ‌ها